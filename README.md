# blindfold-chess
Terminal based blindfolded chess trainer. Play standard algebraic notation (e4, Nf3) against a computer engine.

## prerequisites (macos)
this tool requires the stockfish binary to run the ai.

`brew install stockfish`

## run locally
`pip install chess`